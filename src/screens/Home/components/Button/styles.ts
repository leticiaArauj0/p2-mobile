import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        borderRadius: 10,
        backgroundColor: '#910AF7',
        padding: 15,
        marginBottom: 15,
    },

    text: {
        fontSize: 16,
        fontWeight: '600',
        color: '#ffffff',
        textAlign: 'center',
        textTransform: 'uppercase',
    }
})
